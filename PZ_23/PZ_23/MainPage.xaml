<Page
    x:Class="PZ_23.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PZ_23"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="12">
            <TextBox x:Name="txtUrl" 
                     PlaceholderText="Введите адрес (например, https://example.com)" 
                     Width="400" Margin="0,0,12,0" KeyDown="txtUrl_KeyDown"/>

            <Button Content="Перейти" Click="BtnGo_Click" Margin="0,0,12,0"/>

            <Button Content="Добавить в закладки" Click="BtnAddBookmark_Click"/>

            <DropDownButton Content="Закладки" Margin="12,0,0,0">
                <DropDownButton.Flyout>
                    <MenuFlyout x:Name="BookmarksMenu" Placement="Bottom"/>
                </DropDownButton.Flyout>
            </DropDownButton>
        </StackPanel>

        <WebView x:Name="webView" 
                 Grid.Row="1" 
                 Source="https://www.microsoft.com" 
                 NavigationCompleted="WebView_NavigationCompleted"/>
    </Grid>
</Page>